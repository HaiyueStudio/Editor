# Editor Shell instructions

- Shell owns browser layout, contribution hosts, shortcuts and presentation adapters, never product documents.
- Registrations belong to lifecycle scopes; listeners and lazy imports must clean up deterministically.
- Depend only on public Platform, Plugin SDK and UI package exports. Do not add AI/chat/provider behavior.
